import React from 'react';
import NavList from './NavList';

const Navbar = () => {
    return (
        <nav>
            Equation Solver
            <NavList />
        </nav>
    );
}

export default Navbar;