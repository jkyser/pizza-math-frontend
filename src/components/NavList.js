import React from 'react';
import NavElement from './NavElement';

const NavList = () => {
    return (
        <ul>
            <NavElement />
            <NavElement />
            <NavElement />
            <NavElement />
        </ul>
    );
}

export default NavList;